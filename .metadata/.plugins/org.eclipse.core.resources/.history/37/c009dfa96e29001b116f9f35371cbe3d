package com.readtime;

import java.io.BufferedReader;
import java.io.IOException;

import com.google.gson.Gson;

public class Serializer {
	protected String BufferedReaderToJson(BufferedReader reader) throws IOException {
		StringBuilder buffer = new StringBuilder();
		String line;
		while ((line = reader.readLine()) != null) {
			buffer.append(line);
		}
		return buffer.toString();
	}

	protected UserData JsonToJavaObject(BufferedReader reader) throws IOException {
		String javaString = BufferedReadertoJson(reader);
		UserData data = new Gson().fromJson(javaString, UserData.class);
		return data;
	}

	protected String JavaObjectToJson(ReadTimeData data) {
		return new Gson().toJson(data);
	}
}
